import React, { Component } from 'react';
import { Grid, Cell } from 'react-mdl'

class Home extends Component {
    render() {
        return(
            <div style={{width: '100%', width: 'auto'}}>
                <Grid className="home-grid">
                    
                </Grid>
            </div>
        )
    }
}

export default Home